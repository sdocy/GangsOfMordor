using UnityEngine;
using System.Collections;

public class captainsquarters : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GEM.trigger_EnterArea(new GEA { area_name = "captainsquarters" });
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
